# Open WebUI on port 8081

This Home Assistant add-on runs the existing Open WebUI image without host
networking and maps its container port 8080 to host port 8081. It avoids the
port conflict with the UniFi add-on.
